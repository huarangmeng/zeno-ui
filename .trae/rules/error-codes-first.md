# Error Codes First

## 新增错误场景时必须先定义错误码
- 新增错误场景时，必须先补充 `ZenoErrorCode`，再落错误对象、日志调用点和文档语义，禁止直接扩散裸字符串错误。
- 同类错误跨模块必须优先复用已有错误码，不重复发明含义接近但名字不同的错误码字符串。
- 如果需要表达更细的错误上下文，优先扩展 `operation` 或附加字段，而不是轻易改动既有错误码。
- 错误日志、错误对象与文档语义必须围绕同一份错误码表保持一一对应，确保可搜索、可观测、可回溯。
