# Verification Zero Warning

## 每次修改必须完成对应验证且零 warning / error
- 每个修改完成后都必须执行与改动范围对应的测试、检查或编译，不能跳过验证直接视为完成。
- 验证范围应尽量覆盖实际影响面；至少执行能证明本次修改正确性的最小充分检查。
- 只有在结果没有任何 warning 和 error 时，改动才算成功；存在未处理告警或编译/测试失败时，不得宣告完成。
